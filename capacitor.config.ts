import { CapacitorConfig } from "@capacitor/cli";

const config: CapacitorConfig = {
  appId: "ninja.schmider.snufftoast",
  appName: "SnuffToast",
  webDir: "dist/SnuffToast",
  bundledWebRuntime: false
};

export default config;
